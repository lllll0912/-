# 健康档案

## 数据放哪

| 内容 | 位置 | 说明 |
|------|------|------|
| 检验单原件（照片/PDF） | `医疗/数据/`（进 Git + 进 Docker 镜像） | **不写 Fly 云盘**，不另占 Volume |
| 目录索引 catalog | 同上；正式站改标注时写到 Volume 里很小的 JSON | 只有标注可写 |
| 看病目的 / 正常异常 | 日历抽屉里编辑 | 保存后进小 JSON |

正式站以前读 `/data/health`（空），所以看不到 2024/2025；现已改为读镜像里的 `医疗/数据`。

## 页面

`/health/calendar` — 医疗日历（主入口）
