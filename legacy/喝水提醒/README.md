# 喝水提醒（桌面小窗）

Windows 悬浮窗 + 系统弹窗提醒。与网站版**共用** `data/water_data.json`。

## 怎么启动

在仓库**根目录**双击：

`启动喝水小窗.bat`

或本目录 `run.bat`。

## 数据

| 路径 | 说明 |
|------|------|
| `../../data/water_data.json` | 与网站共用的正式数据 |
| 本目录旧 `data/water_data.json` | 仅归档，启动后不再写入 |

网站「历史」页应能看到 7 月 9 日以来的记录；小窗打卡会同步进同一文件。
