# 目录与配置

你日常只需要看根目录这几样：

```text
医疗/  喝水/  诗词/  日志/  账单/  收藏/  脚本/
README.md
需求记录.md
目录与配置.md
VERSION.md
```

带 `.` 开头的（如 `.git` `.env`）是系统和部署用的，资源管理器里可隐藏，**不用打开、不用改**。

## 功能文件夹

| 文件夹 | 里面有什么 |
|--------|------------|
| `医疗/` | 医疗日历代码 + `数据/`（检验单、catalog，进私密 Git） |
| `喝水/` | 喝水网站 + `启动喝水小窗.bat` + `数据/` |
| `诗词/` | 诗词功能 + `数据/` |
| `日志/` | 笔记专栏 + `数据/` |
| `账单/` | 账单/旅游 + `数据/` |
| `收藏/` | 作品/人物收藏 + `数据/pics`（自 teachersql 迁入） |
| `脚本/` | 启动网站、部署、工具、全站壳；你一般只点 `启动网站.bat` |

## 本机启动

双击：`脚本/启动网站.bat` → http://127.0.0.1:8501

医疗日历：http://127.0.0.1:8501/health/calendar

## 数据与 GitHub（私密仓）

各功能下的 `数据/` **会提交到 GitHub**，与本地保持一致。  
`.env`（密码）仍不提交。

正式站 Fly 仍用 Volume `/data`（与推代码不互相覆盖的契约不变）。

## 部署

配置与 Dockerfile 在 `脚本/`；GitHub Actions 自动部署。日常 `git push` 即可。
