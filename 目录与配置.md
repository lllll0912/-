# 目录与配置

你日常只需要看根目录这几样：

```text
医疗/  喝水/  诗词/  日志/  账单/  收藏/  脚本/
README.md
需求记录.md
目录与配置.md
VERSION.md
```

带 `.` 开头的（如 `.git` `.env`）是系统和部署用的，资源管理器里可隐藏，**不用打开、不用改**。

## 功能文件夹

| 文件夹 | 里面有什么 |
|--------|------------|
| `医疗/` | 医疗日历代码 + `数据/`（检验单、catalog，进私密 Git） |
| `喝水/` | 喝水网站 + `启动喝水小窗.bat` + `数据/` |
| `诗词/` | 诗词功能 + `数据/` |
| `日志/` | 笔记专栏 + `数据/` |
| `账单/` | 账单/旅游 + `数据/` |
| `收藏/` | 作品/人物收藏 + `数据/`（**运行态不进 Git**，见下） |
| `脚本/` | 启动网站、部署、工具、全站壳；你一般只点 `启动网站.bat` |

## 本机启动

双击：`脚本/启动网站.bat` → http://127.0.0.1:8501

医疗日历：http://127.0.0.1:8501/health/calendar

## 数据与 GitHub（私密仓）

各功能下的 `数据/` **会提交到 GitHub**，与本地保持一致（**收藏模块除外**，见下）。  
`.env`（密码）仍不提交。

正式站 Fly 仍用 Volume `/data`（与推代码不互相覆盖的契约不变）。

### 数据架构约定（省 Fly 存储 + 全量 GitHub 备份）

以后**新开发任何页面/模块**，默认遵循：

| 阶段 | 做什么 |
|------|--------|
| **1. 新功能开发** | 本机 `数据/` 造测试数据；需要时可 **一次性** `git add -f` 种子数据上线 |
| **2. 正式站稳定后** | **只在正式站改数据** → 经 GitHub API / Volume 备份写入仓库 |
| **3. 本机 thereafter** | 本地 `数据/` **仅开发测试**；`git push` **只推代码**，不推运行态 |

| 类型 | 存哪 | GitHub 怎么备 |
|------|------|----------------|
| **文字 / 结构化数据**（评分、说明、目录 JSON、SQLite 等） | Fly Volume `/data` 运行态 | **不实时 commit**；由 Actions **每日**把 Volume 快照打进 `backups/` |
| **图片 / 视频等大文件** | **GitHub 为真相仓**（如 `收藏/数据/pics/`） | 正式站上传/抓图时 commit；Fly Volume **只做短缓存** |
| **网站加载** | 先读 Volume 缓存 → 未命中再从 GitHub 拉 | pics 用索引缓存，避免每张图打 API |

**收藏（作品/人物）—— 本地 vs 正式站：**

| 路径 | 正式站 | 本机 `git push` |
|------|--------|-----------------|
| `收藏/collection/` 代码 | deploy | ✅ 推 |
| `收藏/数据/pics/` | 上传时 API commit（不触发 deploy） | ❌ **已 .gitignore** |
| `收藏/数据/_meta/catalog.json` 等 | **只写 Volume**，靠日备 `backups/` | ❌ **已 .gitignore** |

本机已对已追踪的 pics/catalog 设置 **skip-worktree**：本地改动不进 commit，**也不会从 GitHub 删远端文件**。换电脑后可再跑 `脚本/工具/收藏-停止Git追踪本地数据.bat`。

定时备份 `backups/*/site_data.tgz`：含 Volume 里的 `collection/`（评分/catalog 的主备份通道）。

**本机开发**：写 `收藏/数据/` 只影响 localhost；**不会**随普通 `git push` 改正式站数据。

## 部署

配置与 Dockerfile 在 `脚本/`；GitHub Actions 自动部署。日常 `git push` 即可。  
`收藏/数据/**`、`医疗/数据/**`、`backups/**` 的变更 **不触发** Fly deploy（避免点星刷部署）。
