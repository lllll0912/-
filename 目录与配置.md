# 目录与配置

根目录应保持精简：入口代码、三份说明 MD、部署必需文件、以及按功能分类的文件夹。

## 根目录文件说明

| 路径 | 说明 |
|------|------|
| `README.md` | 项目说明与访问地址 |
| `需求记录.md` | 历次需求原文、时间、方案、结果 |
| `目录与配置.md` | 本文件：目录大纲与环境变量 |
| `app.py` / `auth.py` | Flask 入口与登录/角色 |
| `run.bat` / `run.sh` | 本机启动 |
| `requirements.txt` | Python 依赖 |
| `.env.example` | 配置样例（复制为 `.env`，勿提交 `.env`） |
| `Dockerfile` / `fly.toml` | Fly 构建（须在根目录） |
| `启动喝水小窗.bat` | 可选：本机 Tk 小窗（与网站共用喝水 JSON） |

## 目录大纲

```text
modules/
  bills/          账单：SQLite 访问、导入解析、规则
  poems/          诗词维护逻辑
  water/          喝水（网站）
  notes/          笔记专栏
  travel/         旅游说明
templates/        页面模板
static/           CSS 等
tools/            离线脚本
tests/            单元测试
deploy/           手动部署/同步脚本
docs/             归档文档（非现行三 MD）
legacy/           旧独立项目归档
poems_data/       诗词库文本/JSON（本地）
data/             运行时隐私数据（不进业务提交，靠备份 workflow）
  bills.db
  notes.db
  water_data.json
  notes_assets/   笔记图片
backup/           本机导出落点
backups/          GitHub 定期备份落点（workflow 写入）
.github/workflows/
```

## 三个数据库（数据面）

### 部署安全契约（必读）

**推上线只更新代码，绝不抹掉开发期间你在正式站写入/调整过的数据。**

| | 代码（Git / 镜像） | 数据（Fly Volume `/data`） |
|--|-------------------|---------------------------|
| `git push` 部署 | 会更新 | **不碰** |
| 正式站导入账单、改字典、旅游打标、笔记、喝水 | 不进 Git | 写在这里，一直保留 |
| 本机 `data/` | 不提交 | 仅本地调试；要对齐线上请「拉取」而不是「上传」 |

持久路径一览：

| 内容 | 本地 | Fly |
|------|------|-----|
| 账单 | `data/bills.db` | `/data/bills.db` |
| 类型字典 | `data/category_rules.json` | `/data/category_rules.json` |
| 诗词 | `poems_data/` | `/data/poems/` |
| 笔记 | `data/notes.db` + `notes_assets/` | `/data/notes.db` + `/data/notes_assets/` |
| 喝水 | `data/water_data.json` | `/data/water_data.json` |
| 自动备份 | `backup/` | `/data/backup/` |

仓库里的 `modules/bills/category_rules.json`、`poems_data/` 等只是**种子**：Volume 上已有对应文件时，启动/部署**不会覆盖**。

本地要按线上现状开发时：

```text
deploy\fly\拉取线上数据到本机.ps1
```

或只拉字典：`tools\sync_rules_from_backup.py` / `deploy\fly\拉取线上类型字典.ps1`。

⚠ `deploy\fly\同步数据库.ps1`、`同步喝水数据.ps1` 是**本机盖到线上**的应急手段，会抹云端数据，日常不要用。

账单类型为**单层**：字典与记录只有「类型」；库内 `category` 与 `category_l1` 双写同值（兼容旧字段）。旧二级名映射见 `modules/bills/legacy_l2_map.json`。

另：喝水记录为 JSON，不算第四「业务库」，但会进入定期备份包。

### 备份落点（重要）

| 环境 | 路径 |
|------|------|
| 本机运行 | 项目根 `backup/`（直接写入，**保留历史**，不删旧文件） |
| Fly 正式站 | Volume `/data/backup/` + 浏览器「绑定本机 backup」自动同步（**追加写入，不删历史**） |

侧栏「绑定本机 backup」选一次项目里的 `backup/` 即可（Chrome/Edge）。未绑定可「导出备份」下 zip。

### 笔记图片命名与显示

- 目录：`notes_assets/{note_id}/`
- 文件名：`{yyyyMMdd_HHmmss}_{8位hex}.{ext}`  
  例：`20260727_153012_a3f91c2b.webp`
- 允许：`jpg/jpeg/png/gif/webp`；单张建议 ≤ 5MB
- 访问：`GET /notes/assets/<note_id>/<filename>`（游客只读；所有者可上传）
- 正文：Markdown `![](/notes/assets/12/20260727_153012_a3f91c2b.webp)`

## 环境变量 / 配置

| 变量 | 说明 |
|------|------|
| `BILL_ACCESS_PASSWORD` | 所有者模式密码 |
| `BILL_SECRET_KEY` | Session 密钥 |
| `BILL_COOKIE_SECURE` | HTTPS 时 `1`；本机 HTTP 用 `0` |
| `BILL_DATA_DIR` | 数据根；Fly 为 `/data` |
| `BILL_DB_PATH` | 可选，覆盖账单库路径 |
| `POEM_ADMIN_PASSWORD` | 诗库维护密码（可回退访问密码） |
| `POEM_LLM_*` | 可选诗境 LLM |
| `FLY_API_TOKEN` | GitHub Secrets：部署与备份拉取 Volume |

本机：复制 `.env.example` → `.env` 后填写。

## 备份与部署 workflow

| 文件 | 作用 |
|------|------|
| `.github/workflows/deploy-bill-fly.yml` | `main` 推送后部署 Fly |
| `.github/workflows/backup-fly-data.yml` | 每日（及手动）把 `/data` 打包提交到 `backups/` |

根目录现行说明 MD 仅三份：`README.md` / `需求记录.md` / `目录与配置.md`。其它 `docs/`、`modules/*/README.md` 为模块或归档说明，不替代上述三份。
