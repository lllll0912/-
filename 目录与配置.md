# 目录与配置

你日常只需要看根目录这几样：

```text
医疗/  喝水/  诗词/  日志/  账单/  收藏/  脚本/
README.md
需求记录.md
目录与配置.md
VERSION.md
```

带 `.` 开头的（如 `.git` `.env`）是系统和部署用的，资源管理器里可隐藏，**不用打开、不用改**。

## 功能文件夹

| 文件夹 | 里面有什么 |
|--------|------------|
| `医疗/` | 医疗日历代码 + `数据/`（检验单原件在 GitHub；标注在 Volume） |
| `喝水/` | 喝水网站 + `启动喝水小窗.bat` + `数据/`（本地仅测试） |
| `诗词/` | 诗词功能 + `数据/`（本地仅测试） |
| `日志/` | 笔记专栏 + `数据/`（本地仅测试） |
| `账单/` | 账单/旅游 + `数据/`（本地仅测试） |
| `收藏/` | 作品收藏 + `数据/`（本地仅测试） |
| `脚本/` | 启动网站、部署、工具、全站壳；你一般只点 `启动网站.bat` |

## 本机启动

双击：`脚本/启动网站.bat` → http://127.0.0.1:8501

## 数据架构（GitHub 要有全量新数据）

| 类型 | 生产真相 | GitHub 上怎么保证全量新数据 |
|------|----------|------------------------------|
| **文字 / DB**（账单、喝水、日志、诗词、收藏 catalog、医疗标注…） | Fly **Volume `/data`** | Actions **每日**打包进 `backups/*/site_data.tgz`（含 collection、health） |
| **照片 / 视频 / 检验单大图** | **GitHub 仓库路径**（正式站上传时 API commit） | 仓库内实时有最新大文件；Volume 只做短缓存 |

**本机 `*/数据/`：** 一律 **.gitignore + skip-worktree**，只作开发测试。  
`git push` **只推代码**，不会用落后的本地库盖掉 GitHub / 不会动 Volume。

换电脑或本地又冒出数据改动时，跑：

`脚本/工具/全站-本地运行态勿推GitHub.bat`

## 部署

配置与 Dockerfile 在 `脚本/`；改代码后 `git push` → Actions 自动 `fly deploy`。  
各模块 `*/数据/**` 与 `backups/**` 的变更 **不触发** 部署。
