# 需求记录

按时间倒序。每条保留：**原始需求**、**提出时间**、**开发方案**、**结果**。

---

## 2026-07-27 · 所有者/游客双模式 + 笔记专栏 + 三库与备份 + 文档三分

### 原始需求（原文要点）

1. 网站打开时选择所有者模式（带密码）和游客模式（无需密码）；所有者可浏览全部；游客只能访问诗词界面（仅查看）和笔记专栏界面（仅查看）；需开发笔记专栏。
2. 开发笔记专栏：增删查改；编辑为 md 格式，图文轻量化编辑，交互风格接近飞书文档。
3. 本地项目全量上 GitHub；网站数据库定期全量打包到 GitHub 备份；仓库私密，可基于 GitHub 远程协作开发。
4. 网站应有三个数据库：账单、诗词、笔记；需明确笔记图片如何保存、显示与命名。
5. 需求管理用 md 记录：每次需求写清原始内容、提出时间、方案与结果；README 严格拆成三个 md：需求记录 + 根目录文件说明及配置项 + 项目说明（含访问地址与大致介绍）。

### 提出时间

2026-07-27

### 开发方案

| 项 | 方案 |
|----|------|
| 双模式 | 入口页选「所有者（密码）」/「游客」；`session.access_role=owner\|guest`；游客仅 `poems` 只读 + `notes` 只读 |
| 笔记 | 独立 SQLite `data/notes.db`；Blueprint `/notes`；Vditor 所见即所得/MD 双模编辑 |
| 图片 | 存 `data/notes_assets/{note_id}/{yyyyMMdd_HHmmss}_{8位hex}.{ext}`；经 `/notes/assets/...` 鉴权输出；编辑器上传插入 `![](url)` |
| 三库 | 账单 `bills.db`；诗词 `poems_data/`（Fly 上 `/data/poems`）；笔记 `notes.db` + 图片目录 |
| 备份 | GitHub Action 定时从 Fly Volume 拉取三库相关文件，打包提交到 `backups/`（仅私密仓） |
| 文档 | 根目录仅保留三份：`需求记录.md`、`目录与配置.md`、`README.md` |

### 结果

- [x] 三份根 MD：`README.md` / `需求记录.md` / `目录与配置.md`（`VERSION.md` 已迁入 `docs/archive/`）
- [x] 所有者/游客入口（`/login`）与全局路由守卫；游客仅诗词只读 + 笔记只读
- [x] 笔记专栏：`modules/notes` + `/notes` CRUD；Vditor IR 编辑；图片 `notes_assets/{id}/{yyyyMMdd_HHmmss}_{8hex}.{ext}`
- [x] `.github/workflows/backup-fly-data.yml` 定时/手动备份到 `backups/`
- [ ] 上线：待你确认后 `git push origin main` → Actions 部署 Fly；备份需仓库已配置 `FLY_API_TOKEN`

---

## 2026-07-24 · 个人网站模块化 / 喝水 Web / 网页小窗 / 上线

（摘要；更早细节见 git 历史）

- 仓库根改造为个人网站；`modules/` + `legacy/`
- 喝水网站版、网页小窗、历史合并；Fly 部署与喝水 JSON 同步
- 文档曾用 `VERSION.md` 等，现已收敛为根目录三 MD 体系
