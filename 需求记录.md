# 需求记录

按时间倒序。每条保留：**原始需求**、**提出时间**、**开发方案**、**结果**。

---

## 2026-08-25 · 日历卡住修复 + 六文件夹整理 + 医疗数据进仓

### 原始需求（原文要点）

1. 点日期界面卡住、不弹材料。
2. 医疗数据迁到当前目录，私密 GitHub 与本地数据一致。
3. 一级目录只留：喝水、诗词、日志、账单、医疗、脚本 + 3 个 MD；讨厌根目录一堆点和杂文件。

### 提出时间

2026-08-25

### 开发方案

| 项 | 方案 |
|----|------|
| 日历 | 去掉巨型 data-records 属性；JSON 脚本块；遮罩用内联 CSS + hc-open，避免缓存导致遮罩常驻 |
| 目录 | 六功能中文夹；基础设施进 `脚本/`；各功能自带 `数据/` |
| Git | 功能 `数据/` 可提交；`.env` 仍忽略；点文件建议资源管理器隐藏 |

### 结果

- [x] 日历点击/关闭修复
- [x] 医疗数据在 `医疗/数据/`
- [x] 根目录六夹 + 三 MD

---

## 2026-08-25 · 医疗日历 + 功能自包含目录

### 原始需求（原文要点）

1. 要「医疗日历」：按年展示 12 个月，一眼看出哪月有检查、哪次异常；可点开预览原件并下载。
2. 根目录太乱：不要 Lib / static / .sh 堆在第一层；MD 说明留根目录；每个功能一个文件夹，相关组件（含喝水 bat）都进该功能目录。

### 提出时间

2026-08-25（傍晚）

### 开发方案

| 项 | 方案 |
|----|------|
| 日历 | `/health/calendar?year=`；12 月网格 + 年条带；日格按 result_status 着色 |
| 预览 | 点击日期抽屉：列表 + 原件预览 / 下载 / 进详情标异常 |
| 异常 | 详情页手标 `unknown/normal/abnormal`（OCR 前可用） |
| 目录 | `modules/<功能>/` 含模板与脚本；全站壳进 `site/`；`启动网站.bat`；喝水 bat 进 water；清根目录误放的 Lib/Scripts |

### 结果

- [x] 医疗日历本地可预览
- [x] 功能目录自包含重组
- [ ] 指标 OCR 自动标异常（后续）

---

## 2026-08-25 · 目的标签 + 健康档案进站 + 模板按功能拆分

### 原始需求（原文要点）

1. 先给医疗档案打看病目的标签，完善数据基建。
2. 再集成进网站做本地预览。
3. 按功能把网站拆成独立文件夹（喝水、账单、医疗、诗词等），根目录/模板太乱。

### 提出时间

2026-08-25（傍晚）

### 开发方案

| 项 | 方案 |
|----|------|
| 标签 | `catalog.json` v2：`purpose` / `purpose_note` / `event_id`；`purposes.json` + `watchlist.json` |
| 网站 | `modules/health` + `/health`（仅所有者）；侧栏「健康」 |
| 数据 | robocopy 桌面档案 → `data/health/`（gitignore） |
| 目录 | `templates/{bills,water,poems,notes,health,auth}/`；`render_template` 路径同步 |

### 结果

- [x] 目的标签与事件分组
- [x] 本地 `/health` 总览 / 时间线 / 档案库 / 详情
- [x] 模板与模块按功能分子目录
- [ ] 指标 OCR / 趋势页后续

---

## 2026-08-25 · 医疗档案本地基建（暂不改网站）

### 原始需求（原文要点）

1. 新增所有者可访问的医疗目录界面（后续）。
2. 基于个人医疗检查单 PNG，按名称/检查时间标注检查名称与项目，并识别指标与值。
3. **当前阶段**：先别改网站；先在本地把医疗相关数据梳理清楚，建好数据基建。

### 提出时间

2026-08-25（下午）

### 开发方案

| 项 | 方案 |
|----|------|
| 范围 | 仅桌面 ``医疗/``；原文件夹保留；规范副本写入 ``医疗/档案/`` |
| 归类 | 本人检验单 / 中药方 / 体检病史 / 张悦医疗 分目录 |
| 命名 | ``YYYY-MM-DD_检查名_医院.ext``；微信 mmexport 逐张识图改名 |
| 索引 | ``档案/_meta/catalog.json`` + ``schema.md`` + 检验时间线；指标 OCR 标为 pending |

### 结果

- [x] 本地档案树与 catalog 已建（网站未改）
- [x] 产品设计写入 `桌面/医疗/档案/_meta/产品设计-健康档案室.md`（异常+关注指标 OCR、目的标签、时间线/趋势/维护）
- [ ] 指标 OCR / 网站模块待下一步

---

## 2026-08-18 · 手机端日志显示不全 + 支出排行列表错位

### 原始需求（原文要点）

1. 手机访问生活日志只看到大约 2 天。
2. 分析看板降序表不能自适应：类型列极窄，事项挤成一列超长文字，单元格被向下拉得很长。

### 提出时间

2026-08-18（傍晚）

### 开发方案

| 项 | 方案 |
|----|------|
| 日志 | 去掉日记卡片 overflow 裁切；小屏关闭 backdrop-filter，避免 iOS 只绘出前几张卡 |
| 排行 | 桌面表格固定列宽 + 事项芯片换行；小屏改为卡片：类型/金额一行，事项芯片，累计条 |
| 顺带 | 去掉侧栏「离线报告包」入口与对应下载路由 |

### 结果

- [x] 手机端日志/支出排行布局修复上线；去掉离线报告包入口

---

## 2026-08-18 · 分析看板改为当月支出排行

### 原始需求（原文要点）

1. 去掉分析看板「类型 - 方向 - 日期（月）汇总」。
2. 改为展示某月降序支出项，看钱主要花在哪。
3. 三列：时间（年-月维度）、事项、金额、累计支出金额（从上往下累加，末行 100%）。

### 提出时间

2026-08-18（上午）

### 开发方案

| 项 | 方案 |
|----|------|
| 时间维度 | 年-月下拉（默认该年最近有支出的月份） |
| 分组 | 按类型汇总、金额降序；事项列为该类型下去重合集（顿号拼接） |
| 累计 | 从上往下加总金额与占比，末行校正为 100% |

### 结果

- [x] 当月支出排行上线（按类型汇总，事项合集带金额，累计到 100%）

---

## 2026-08-03 · 笔记手机编辑区 / 时间排序 / Fly 费用控制说明

### 原始需求（原文要点）

1. 手机端编辑笔记时编辑窗口太小。
2. 系统时间不对，笔记编辑时间错；列表要能按创建时间、编辑时间排序。
3. Fly 云部署会花钱，不清楚钱怎么花、被攻击账单暴涨怎么办、如何控制。

### 提出时间

2026-08-03（上午）

### 开发方案

| 项 | 方案 |
|----|------|
| 手机编辑 | 视口高度动态计算 Vditor；小屏 CSS `min-height: 70dvh` |
| 时间 | 新写入用上海时区；Fly Volume 上历史 UTC 墙钟一次性 +8 校正 |
| 排序 | 列表提供「按编辑时间 / 按创建时间」切换 |
| 费用 | 说明计费构成与控制手段（Dashboard 限额/告警、小机型、auto-stop、密码门、可选 CDN）；不改部署架构 |

### 结果

- [x] 手机编辑区放大 + 上海时区校正 + 创建/编辑排序上线
- [x] Fly 费用与防刷说明见对话回复

---

## 2026-07-29 · 喝水页时间校正 + 激励视觉

### 原始需求（原文要点）

1. 记录明细日期对、时间不对。
2. 页面应显示当前时间、下次提醒时间，不要太光秃。
3. 视觉要激励喝水、看着就想喝。

### 提出时间

2026-07-29（下午）

### 开发方案

| 项 | 方案 |
|----|------|
| 时区 | 统一 `Asia/Shanghai`；Fly 上无时区历史记录按 UTC→上海；新记录带上海时区写入 |
| 展示 | 主页/小窗：当前时间、下次提醒、倒计时；明细用校正后的时分秒 |
| 视觉 | 水色渐变舞台、水杯进度、随进度变化的激励文案、大号 CTA |

### 结果

- [x] 时区校正 + 激励页/小窗上线

---

## 2026-07-28 · 笔记速查栏 + 全量备份（含喝水/诗词/笔记）

### 原始需求（原文要点）

1. 笔记编辑工具栏太丑、蓝底看不清；不熟 MD，要把标题/加粗/序号等放在编辑界面，且速查栏可自行增改。
2. 备份应包含喝水、诗词等全部数据，防止网站不可用时没有全量备份。

### 提出时间

2026-07-28（下午）

### 开发方案

| 项 | 方案 |
|----|------|
| 工具栏 | 浅底深色图标 CSS；精简 Vditor 工具项 |
| 速查栏 | 编辑页下方可点击插入；「编辑速查」持久化到 `notes_md_hints.json` |
| 全量备份 | 额外写出 water/poems/notes/rules JSON + `*_fulldata.zip`（库与资源）；GitHub volume 备份补上 category_rules |

### 结果

- [x] 笔记工具栏可读性与 Markdown 速查
- [x] 速查可编辑并持久化
- [x] 网站备份覆盖账单以外的主要数据面

---

## 2026-07-28 · 备份保留历史 + 类型字典持久化 / 本地对齐线上

### 原始需求（原文要点）

1. 自动备份绑定本地文件夹时不该删掉历史备份。
2. 线上改完字典后本地开发再推上线，线上调整消失；希望本地基于线上版本开发。
3. 拓展：推上线不应影响任何线上已有数据（开发期间正式站写入的账单/调整不能被抹掉）。

### 提出时间

2026-07-28

### 开发方案

| 项 | 方案 |
|----|------|
| 原则 | 部署只换代码；`/data` Volume 为唯一运行时数据面 |
| 备份历史 | 本机同步与服务器默认均不删旧 `records_backup_*` |
| 字典 | 落到 `/data/category_rules.json`；种子仅首次拷贝；可用 Volume 内最新 `*_types.json` 迁移 |
| 对齐开发 | `拉取线上数据到本机.ps1`；应急「本机→线上」脚本加 YES 确认与警告 |
| 文档 | `目录与配置.md` / `deploy/README.md` 写明部署安全契约 |

### 结果

- [x] 备份保留历史
- [x] 部署不再覆盖线上字典
- [x] 明确「代码 / 数据」分离与拉数脚本

---

## 2026-07-27 · 类型字典批量维护（一次确认才备份）

### 原始需求（原文要点）

类似同行人维护，账单类型字典也一次性编辑很多条后点确认才触发备份，别改一条就备份。

### 提出时间

2026-07-27（傍晚）

### 开发方案

列表只读展示；「类型字典维护」弹窗内增删改支出/收入规则；确认后 `replace_rule_maps` 整表写入，再备份一次。

### 结果

- [x] 弹窗批量维护类型字典
- [x] 确认后才保存并备份

---

## 2026-07-27 · 纯日记（零金额）入库丢失

### 原始需求（原文要点）

没有产生金额的日志记录去哪里了，虽然没有金额，但是有日志。

### 提出时间

2026-07-27（傍晚）

### 开发方案

纯日记天解析为金额 0、明细空，预览校验「类型明细不能为空」→ `is_valid=0` → 确认入库跳过。改为：有日记且金额为 0 时明细填「日记」、类型「零金额」，视为有效并入库。

### 结果

- [x] 纯日记天可入库并出现在生活日志

---

## 2026-07-27 · 旅游管理表格合并与同行人批量维护

### 原始需求（原文要点）

1. 旅游专题分析与「已打标日期」表格功能重复，保留专题分析即可。
2. 表格太丑，优化样式。
3. 取消表内直接改同行人；新增「同行人维护」按钮，弹窗可改多条，确认后全量变更，避免改一条就触发备份。

### 提出时间

2026-07-27（傍晚）

### 开发方案

| 项 | 方案 |
|----|------|
| 去重 | 移除「已打标日期」卡片 |
| 样式 | 专题分析独立表格样式（粘性表头、数字右齐、分区标签） |
| 同行人 | 弹窗批量编辑 → `batch_update_companions` 一次提交 + 一次备份 |

### 结果

- [x] 仅保留旅游专题分析
- [x] 表格样式优化
- [x] 同行人弹窗批量维护

---

## 2026-07-27 · 绑定本机 backup 目录（免整理下载路径）

### 原始需求（原文要点）

能不能设置网站下载到本地的路径直接就是 backup 里面，懒得手动整理路径。

### 提出时间

2026-07-27（傍晚）

### 开发方案

浏览器不能指定系统下载目录。改为：本机运行直接落盘；正式站侧栏「绑定本机 backup」持久化目录句柄，入库/类型/旅游/导出后自动把最新备份文件写入该文件夹；未绑定则提示绑定或下 zip。

### 结果

- [x] 本机：备份直接进项目 `backup/`
- [x] 正式站：可绑定本机 `backup/` 自动写入
- [x] 未绑定回退 zip

---

## 2026-07-27 · 导入零金额 / 旅游日期面板 / 备份落点

### 原始需求（原文要点）

1. 导入清洗：金额为 0 的不用出现在待分类里，不用手动打标签，不需要分类。
2. 旅游管理选日期下半截被挡住，无法选想选的月。
3. 类型字典、旅游管理变动时，也要像账单一样自动备份到 backup（同第 4 点逻辑）。
4. 插入账单后有自动备份提示，但本地看不到文件；希望本机 `backup/` 也有一份。

### 提出时间

2026-07-27（傍晚）

### 开发方案

| 项 | 方案 |
|----|------|
| 零金额 | 预览归一为类型「零金额」；不进待分类/待映射筛选与拦截 |
| 旅游日期 | 级联面板改为 `position:fixed` + 视口定位，列可滚动，避免被裁切 |
| 类型/旅游备份 | 保存/删除类型、打标/清标/改同行人后同样服务器落盘 + 同步本机 |
| 备份落点 | 本机直接写 `backup/`；正式站绑定本机文件夹后自动写入 |

### 结果

- [x] 零金额跳过待分类
- [x] 旅游日期面板定位修复
- [x] 类型/旅游变更自动服务器备份
- [x] 本机绑定 backup 自动写入（见上一条）

---

## 2026-07-27 · 账单类型改为仅一级（去掉二级）

### 原始需求（原文）

先别慌推上线，现在调整一下功能，就是账单的类型，我希望不要搞二级类型了，太复杂，就简单的一级类型就行，帮我全量调整

### 提出时间

2026-07-27（下午）

### 开发方案

| 项 | 方案 |
|----|------|
| 规则 | `category_rules.json` 扁平为 `{类型名: pattern}`；旧嵌套 L2 pattern 合并进对应一级 |
| 历史数据 | 启动一次性迁移：旧二级名按映射归入一级；`category` 与 `category_l1` 双写同值 |
| UI | 类型字典 / 记录 / 导入 / 分析 / 临时表 只保留「类型」一层 |
| 兼容 | 保留 `legacy_l2_map.json`，导入旧「医药」等二级名仍可映射到「生活支出」 |

### 结果

- [x] `rule_manager` 单层化 + 规则文件扁平化
- [x] 本地库折叠迁移（`collapse_categories_to_single_level`）
- [x] 相关模板与导入/分析/类型页去掉二级
- [x] 推送 `main` 上线（Actions → Fly），便于网页版验证导入落库与备份

---

## 2026-07-27 · 所有者/游客双模式 + 笔记专栏 + 三库与备份 + 文档三分

### 原始需求（原文要点）

1. 网站打开时选择所有者模式（带密码）和游客模式（无需密码）；所有者可浏览全部；游客只能访问诗词界面（仅查看）和笔记专栏界面（仅查看）；需开发笔记专栏。
2. 开发笔记专栏：增删查改；编辑为 md 格式，图文轻量化编辑，交互风格接近飞书文档。
3. 本地项目全量上 GitHub；网站数据库定期全量打包到 GitHub 备份；仓库私密，可基于 GitHub 远程协作开发。
4. 网站应有三个数据库：账单、诗词、笔记；需明确笔记图片如何保存、显示与命名。
5. 需求管理用 md 记录：每次需求写清原始内容、提出时间、方案与结果；README 严格拆成三个 md：需求记录 + 根目录文件说明及配置项 + 项目说明（含访问地址与大致介绍）。

### 提出时间

2026-07-27

### 开发方案

| 项 | 方案 |
|----|------|
| 双模式 | 入口页选「所有者（密码）」/「游客」；`session.access_role=owner\|guest`；游客仅 `poems` 只读 + `notes` 只读 |
| 笔记 | 独立 SQLite `data/notes.db`；Blueprint `/notes`；Vditor 所见即所得/MD 双模编辑 |
| 图片 | 存 `data/notes_assets/{note_id}/{yyyyMMdd_HHmmss}_{8位hex}.{ext}`；经 `/notes/assets/...` 鉴权输出；编辑器上传插入 `![](url)` |
| 三库 | 账单 `bills.db`；诗词 `poems_data/`（Fly 上 `/data/poems`）；笔记 `notes.db` + 图片目录 |
| 备份 | GitHub Action 定时从 Fly Volume 拉取三库相关文件，打包提交到 `backups/`（仅私密仓） |
| 文档 | 根目录仅保留三份：`需求记录.md`、`目录与配置.md`、`README.md` |

### 结果

- [x] 三份根 MD：`README.md` / `需求记录.md` / `目录与配置.md`（`VERSION.md` 已迁入 `docs/archive/`）
- [x] 所有者/游客入口（`/login`）与全局路由守卫；游客仅诗词只读 + 笔记只读
- [x] 笔记专栏：`modules/notes` + `/notes` CRUD；Vditor IR 编辑；图片 `notes_assets/{id}/{yyyyMMdd_HHmmss}_{8hex}.{ext}`
- [x] `.github/workflows/backup-fly-data.yml` 定时/手动备份到 `backups/`
- [x] 已推送 `main`（`24fa6cd`）与本地对齐；Actions 将自动部署 Fly；备份需仓库 Secrets 含 `FLY_API_TOKEN`

---

## 2026-07-24 · 个人网站模块化 / 喝水 Web / 网页小窗 / 上线

（摘要；更早细节见 git 历史）

- 仓库根改造为个人网站；`modules/` + `legacy/`
- 喝水网站版、网页小窗、历史合并；Fly 部署与喝水 JSON 同步
- 文档曾用 `VERSION.md` 等，现已收敛为根目录三 MD 体系
